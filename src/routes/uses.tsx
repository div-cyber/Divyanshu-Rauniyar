import { ComingSoon } from "../components/coming-soon";

export default function UsesPage() {
  return (
    <ComingSoon
      eyebrow="Uses"
      title="My setup, soon."
      description="A live list of the hardware, editors, terminal setup, and AI tools I actually use. Coming with the next update."
    />
  );
}
